## What this PR changes

<!-- One paragraph: the user-visible behavior change or the technical fix. -->

## Why

<!-- The problem this solves. Link to an issue if there is one. -->

## Test plan

- [ ] `dotnet test tests/QType.Tests/` passes
- [ ] SDK builds: `cd sdk && npm run build && npm test`
- [ ] MCP builds: `cd mcp && npm run build`
- [ ] Manually verified the behavior change with: `<paste API call or sample>`

## Notes for reviewers

<!-- Anything non-obvious. Edge cases, known limitations, follow-ups. -->
